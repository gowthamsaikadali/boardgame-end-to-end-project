apiVersion: apps/v1
kind: Deployment
metadata:
  name: board-app
  labels:
    app: board-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: board-app
  template:
    metadata:
      labels:
        app: board-app
    spec:
      containers:
      - name: board-app
        image: gowthamacr9l96uw.azurecr.io/boardgameimage:v1
        ports:
        - containerPort: 8080